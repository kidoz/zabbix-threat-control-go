# Zabbix Docker image version tag (e.g. 7.0-latest, 6.0-latest)
ZABBIX_VERSION=7.0-latest

# Zabbix Agent 2 image tag — must be Ubuntu-based (Alpine lacks dpkg)
ZABBIX_AGENT2_VERSION=7.0-ubuntu-latest

# Vulners API key (optional; enables scan/fix testing when set)
# Get yours at https://vulners.com/userinfo
VULNERS_API_KEY=
